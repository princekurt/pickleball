# Local development (SQLite)
DATABASE_URL="file:./dev.db"

# Vercel production — set in Vercel dashboard (PostgreSQL required)
# DATABASE_URL="postgresql://user:password@host:5432/pickleball?sslmode=require"

# Optional: comma-separated allowed client origins for CORS
# CLIENT_ORIGIN=https://your-client.vercel.app
