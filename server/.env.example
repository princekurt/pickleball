# PostgreSQL connection string (local, Neon, Vercel Postgres, etc.)
DATABASE_URL="postgresql://user:password@localhost:5432/pickleball?schema=public"

# Optional: comma-separated allowed client origins for CORS
# CLIENT_ORIGIN=https://your-client.vercel.app
